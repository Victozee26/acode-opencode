// The OpenCode web UI allows users to open and switch between any folders
// at runtime — no server restart is needed. Project path resolution is no
// longer required by the core flow.
//
// This module is intentionally kept (rather than deleted) as a stub for future
// SAF-bridging: on Android, Acode accesses files through the Storage Access
// Framework, so mapping an Acode editor path to a real filesystem path that the
// Alpine-terminal OpenCode can read will eventually need dedicated logic here.
// Until then it stays as a placeholder to avoid breaking the import surface.
