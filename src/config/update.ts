// Command to read the currently installed opencode-ai CLI version.
export const VERSION_CHECK_COMMAND = 'opencode --version';

// Command to query the latest available opencode-ai version from npm.
export const LATEST_VERSION_COMMAND = 'npm view opencode-ai version';

// Command to install (or update) the opencode-ai npm package globally.
export const INSTALL_UPDATE_COMMAND = 'npm install -g opencode-ai';
