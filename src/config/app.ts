// Legacy master switch for verbose debug logging. Runtime console output is now
// controlled by the native `logLevel` plugin setting (set to 'none' to disable);
// this constant is kept for backward compat and as the compile-time default.
export const DEBUG = false;
